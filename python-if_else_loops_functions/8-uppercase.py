#!/usr/bin/python3
def uppercase(str):
    caracter
    for i in str:
        if str >= 'a' and str <= 'z':
            caracter = chr(ord(caracter) - 32)
        elif 'a' <= str <= 'z':
            caracter = chr(ord(caracter) + 32)

        resultado += str
    print("{}".format(resultado))
